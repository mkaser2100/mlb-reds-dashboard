--- a/app-v4.js
+++ b/app-v4.js
@@
 function selectedGamePitcherRow(side = selectedPitcherSide) {
   // A home-team pitcher faces away hitters; an away-team pitcher faces home hitters.
   const opposingHitterSide = side === "away" ? "home" : "away";
-  return (
-    teamGameRows.find(
-      (row) =>
-        row.hitter_side === opposingHitterSide &&
-        row.pitcher_id
-    ) ||
-    teamGameRows.find((row) => row.pitcher_id) ||
-    null
-  );
+  return (
+    teamGameRows.find(
+      (row) =>
+        row.hitter_side === opposingHitterSide &&
+        row.pitcher_id
+    ) ||
+    null
+  );
 }
@@
 function renderMatchupHero() {
   const game = selectedTeamGame();
   const pitchers = selectedGamePitchers();
-  const pitcherRow = pitchers[selectedPitcherSide] || pitchers.home || pitchers.away;
+  const pitcherRow = pitchers[selectedPitcherSide] || null;
@@
-  setText("pitcherNameCard", pitcherRow?.pitcher_name || "Probable Starter TBD");
-  setText("pitcherThrows", pitcherRow?.pitcher_throws || "—");
+  const starterAvailable = Boolean(pitcherRow?.pitcher_id);
+
+  setText("pitcherNameCard", starterAvailable ? (pitcherRow.pitcher_name || "Probable Starter TBD") : "Probable Starter TBD");
+  setText("pitcherThrows", starterAvailable ? (pitcherRow.pitcher_throws || "—") : "—");
   setText(
     "pitcherEra",
-    fmtDecimal(pitcherRow?.pitcher_last5_era, 2)
+    starterAvailable ? fmtDecimal(pitcherRow?.pitcher_last5_era, 2) : "—"
   );
   setText(
     "pitcherWhip",
-    fmtDecimal(pitcherRow?.pitcher_last5_whip, 2)
+    starterAvailable ? fmtDecimal(pitcherRow?.pitcher_last5_whip, 2) : "—"
   );

   const handSplits =
-    mlbTargetPitcherSplits?.[String(pitcherRow?.pitcher_id)] || {};
+    starterAvailable
+      ? (mlbTargetPitcherSplits?.[String(pitcherRow.pitcher_id)] || {})
+      : {};

   setText(
     "pitcherVsLhb",
-    fmtAvg(
-      pitcherRow?.pitcher_baa_vs_lhb ??
-      handSplits?.lhb?.batting_average_against
-    )
+    starterAvailable
+      ? fmtAvg(
+          pitcherRow?.pitcher_baa_vs_lhb ??
+          handSplits?.lhb?.batting_average_against
+        )
+      : "—"
   );
   setText(
     "pitcherVsRhb",
-    fmtAvg(
-      pitcherRow?.pitcher_baa_vs_rhb ??
-      handSplits?.rhb?.batting_average_against
-    )
+    starterAvailable
+      ? fmtAvg(
+          pitcherRow?.pitcher_baa_vs_rhb ??
+          handSplits?.rhb?.batting_average_against
+        )
+      : "—"
   );
 }
